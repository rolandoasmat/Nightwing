<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/home"
    app:startDestination="@+id/homeScreen">

    <fragment
        android:id="@+id/homeScreen"
        android:name="com.asmat.rolando.popularmovies.home.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_homeScreen_to_movieDetailsScreen"
            app:destination="@id/movieDetailsScreen" />
    </fragment>

    <fragment
        android:id="@+id/movieDetailsScreen"
        android:name="com.asmat.rolando.popularmovies.moviedetails.MovieDetailsFragment"
        android:label="Movie Details"
        tools:layout="@layout/fragment_movie_details" >
        <argument
            android:name="movieIdArg"
            app:argType="integer" />
    </fragment>

</navigation>